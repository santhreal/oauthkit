# oauthkit BACKLOG

Single project ledger for the standalone `oauthkit` OAuth SDK. Rows follow the project pattern: `number | affected files | problem | acceptance criteria` with `status` and `tags` for drain tracking.

| number | affected files | problem | acceptance criteria | status | tags |
|--------|----------------|---------|---------------------|--------|------|
